{
  "name": "hermes-backend-link",
  "version": "0.1.0",
  "description": "Client link for the Hermes backend",
  "main": "lib/index",
  "scripts": {
    "test": "NODE_ENV=testing mocha --recursive --exit test/",
    "test:slow": "NODE_ENV=testing mocha --timeout 4000 --recursive --exit test/",
    "test:single": "NODE_ENV=testing mocha --recursive --exit",
    "mocha": "mocha test/ --recursive --exit"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/IOTHermes/hermes-backend-link.git"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@feathersjs/authentication-client": "^1.0.2",
    "@feathersjs/feathers": "^3.1.5",
    "@feathersjs/socketio-client": "^1.1.0",
    "socket.io-client": "^2.1.0"
  },
  "devDependencies": {
    "eslint": "^4.19.1",
    "mocha": "^5.1.1",
    "request": "^2.85.0",
    "request-promise": "^4.2.2"
  }
}
